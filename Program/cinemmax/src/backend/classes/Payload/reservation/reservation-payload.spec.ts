import { ReservationPayload } from './reservation-payload';

describe('ReservationPayload', () => {
  it('should create an instance', () => {
    expect(new ReservationPayload()).toBeTruthy();
  });
});
