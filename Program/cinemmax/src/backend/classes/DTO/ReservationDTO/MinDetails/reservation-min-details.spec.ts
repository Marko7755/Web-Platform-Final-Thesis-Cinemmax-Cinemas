import { ReservationMinDetails } from './reservation-min-details';

describe('ReservationMinDetails', () => {
  it('should create an instance', () => {
    expect(new ReservationMinDetails()).toBeTruthy();
  });
});
