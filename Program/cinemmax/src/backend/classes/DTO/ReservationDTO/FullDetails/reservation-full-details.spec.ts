import { ReservationFullDetails } from './reservation-full-details';

describe('ReservationFullDetails', () => {
  it('should create an instance', () => {
    expect(new ReservationFullDetails()).toBeTruthy();
  });
});
