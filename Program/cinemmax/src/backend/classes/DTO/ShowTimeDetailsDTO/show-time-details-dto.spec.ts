import { ShowTimeDetailsDTO } from './show-time-details-dto';

describe('ShowTimeDetailsDTO', () => {
  it('should create an instance', () => {
    expect(new ShowTimeDetailsDTO()).toBeTruthy();
  });
});
