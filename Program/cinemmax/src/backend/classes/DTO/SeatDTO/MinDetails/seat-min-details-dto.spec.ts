import { SeatMinDetailsDTO } from './seat-min-details-dto';

describe('SeatMinDetailsDTO', () => {
  it('should create an instance', () => {
    expect(new SeatMinDetailsDTO()).toBeTruthy();
  });
});
